$kind: http-request
url: "{{base_url}}/tech-stack"
method: POST
headers:
  Accept: application/json
body:
  type: formdata
  content:
    - type: text
      key: title
      value: Stack 4
    - type: file
      key: image
      src:
        - /C:/Users/Rafsun/Downloads/it-deparment-head.jpeg
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000

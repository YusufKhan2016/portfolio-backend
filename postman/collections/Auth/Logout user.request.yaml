$kind: http-request
url: "{{base_url}}/logout"
method: POST
body:
  type: formdata
  content:
    - type: text
      key: username
      value: Rafsun19
    - type: text
      key: password
      value: "0123456"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000

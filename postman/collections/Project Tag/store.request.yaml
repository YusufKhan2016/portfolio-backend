$kind: http-request
url: "{{base_url}}/project-tag"
method: POST
headers:
  Accept: application/json
body:
  type: formdata
  content:
    - type: text
      key: tag_name
      value: tag 6
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000

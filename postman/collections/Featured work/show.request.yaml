$kind: http-request
url: "{{base_url}}/featured-work/3"
method: GET
body:
  type: formdata
  content:
    - type: text
      key: username
      value: Rafsun19
    - type: text
      key: password
      value: "0123456"
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 3000
